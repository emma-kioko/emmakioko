# PYTHON CODE
import random

numbers = []
even = []
odd = []

for _ in range(20):
    num = random.randint(1, 100)
    numbers.append(num)

    if num % 2 == 0:
        even.append(num)
    else:
        odd.append(num)

print(numbers)
print(even)
print(odd)
